{
  "name": "obsidian-vault-api",
  "version": "1.1.1",
  "description": "Obsidian plugin that exposes your vault as a local MCP server — read, write, search files, view images, and run commands. Compatible with Claude Desktop, LM Studio, Ollama, and any MCP client.",
  "author": "Alexandre Ramos",
  "license": "CC-BY-NC-SA-4.0",
  "keywords": ["obsidian", "obsidian-plugin", "mcp", "model-context-protocol", "claude", "ai", "vault"],
  "scripts": {
    "sync-bridge": "node scripts/sync-bridge.mjs",
    "build": "npm run sync-bridge && node esbuild.config.mjs production",
    "dev": "npm run sync-bridge && node esbuild.config.mjs",
    "typecheck": "npm run sync-bridge && tsc --noEmit",
    "test": "npm run sync-bridge && vitest run",
    "test:watch": "npm run sync-bridge && vitest"
  },
  "devDependencies": {
    "@types/node": "^20.19.41",
    "esbuild": "^0.21.5",
    "obsidian": "^1.12.3",
    "typescript": "^5.9.3",
    "vitest": "^3.1.3"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  },
  "main": "main.js",
  "type": "commonjs"
}
